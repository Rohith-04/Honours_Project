# =============================================================================
# .gitignore — Honours Project
# Covers VCS, Verdi, and general build artefacts.
# run/ is generated on demand by the Makefile; waves/ is tracked source.
# =============================================================================

# -----------------------------------------------------------------------------
# VCS compile and simulation outputs  (per-module subdirs under run/)
# -----------------------------------------------------------------------------
run/
!run/.gitkeep

# Root-level VCS leftovers
*.cm
*.so
*.a
*.o
ucli.key
DVEfiles/

# -----------------------------------------------------------------------------
# Verdi / FSDB waveform outputs
# -----------------------------------------------------------------------------
*.fsdb
*.vpd
*.vdb
verdiLog/

# Verdi session files
novas.conf
novas.rc
novas.*.log*
novas_dump.log
*.ses
*.ses.config
*.ses.png
*.ses.wave.*

# Protect waves/ .rc files — these are tracked source, not artefacts
!waves/
!waves/*.rc

# Catch any stray .rc files at root (Verdi auto-generates these)
/*.rc
/*.conf

# -----------------------------------------------------------------------------
# Synopsys tool files
# -----------------------------------------------------------------------------
*.key

# -----------------------------------------------------------------------------
# Python
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/

# -----------------------------------------------------------------------------
# Editor and OS artefacts
# -----------------------------------------------------------------------------
.DS_Store
Thumbs.db
*.swp
*.swo
*~
*.bak
*.orig
.idea/
.vscode/
*.iml

# -----------------------------------------------------------------------------
# Temporary and log files
# -----------------------------------------------------------------------------
*.log
